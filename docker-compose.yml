version: '3.8'

services:
  api:
    restart: always
    build:
      context: ./api
    env_file:
      - .env
    depends_on:
      - redis
    networks:
      - my-network

  frontend:
    restart: always
    build:
      context: ./frontend
    env_file:
      - .env
    depends_on:
      - api
    networks:
      - my-network

  parser:
    restart: always
    build:
      context: ./parser
    env_file:
      - .env
    depends_on:
      - api
      - selenium-hub
    networks:
      - my-network
    # ports:
    #   - 80:80
    #   - 433:433

  redis:
    restart: always
    build:
      context: ./redis
    env_file:
      - .env
    volumes:
      - ./backup:/backup
    command: sleep infinity
    networks:
      - my-network

  # selenium-hub:
  #   image: selenoid/chrome:latest
  #   networks:
  #     - my-network
  #   ports:
  #     - "4444:4444"

networks:
  my-network:
    driver: bridge
